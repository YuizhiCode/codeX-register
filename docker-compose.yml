services:
  codex-register:
    build: .
    container_name: codex-register
    ports:
      - "${PORT:-8765}:8765"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=8765
    working_dir: /app/data
    restart: unless-stopped
